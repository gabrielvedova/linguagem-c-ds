// Nome: Gabriel Silva Della Vedova 
// Turma: 2A 
//11.Abaixo está o programa C para mostrar erro semântico. Quando uma frase está sintaticamente correta, mas não tem significado, ocorrem erros semânticos. Isso é semelhante aos erros gramaticais. Se uma expressão for inserida no lado esquerdo do operador de atribuição, poderá ocorrer um erro semântico.

#include <stdio.h>

int main()
{
int x = 10;
   b = 20, c;
x + y = c;
printf("%d", c);
   return 0;
}
