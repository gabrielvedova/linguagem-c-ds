// Nome: Gabriel Silva Della Vedova 
// Turma: 2A 
//15.Por segurança, use até 10^7 (10000000 ). No exemplo abaixo mencionamos mais de 10^8, portanto, isso causará um erro devido à grande alocação de memória.

#include <stdio.h>
int main()
{
int ETEPD[10000000000];
printf("%d", ETEPD);
return 0;
}
